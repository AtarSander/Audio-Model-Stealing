defaults:
  - paths: repro
  - target: hubert_base
  - student: wav2vec2_base
  - query_source: librispeech_asr
  - training: stolenencoder
  - downstream: superb_ks
  - runtime: gpu
  - matrix: hubert_default
  - _self_

seed: 42
run_name: null

run:
  step: all

matrix_run:
  step: all
  skip_existing: false
  output_name: matrix_summary

hydra:
  job:
    chdir: false
  run:
    dir: .
  output_subdir: null
